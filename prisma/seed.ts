// ============================================================
// NannyOra — Database Seed Script
// ============================================================
// Intentionally a no-op. The platform is live with real accounts;
// demo/seed data was removed. Do NOT recreate demo users here —
// real nannies/parents sign up through the app, and admin access
// is via the env-backed backup admin (ADMIN_BACKUP_EMAIL /
// ADMIN_BACKUP_PASSWORD), not a seeded DB row.

async function main() {
  console.log("Seed is a no-op — NannyOra runs on real, self-registered accounts.");
}

main();
